> nif unico que me falta
    - unique field
> problema de ter o numero do cliente na tabela cliente assim como o saldo em vez de ter no contrato

> falta me uma tabela de carregamentos
> normalized phone numbers convem ser string
> Permite registar e atualizar o(s) saldo(s) de cada contrato com plano pré-pago?

> Permite registar as chamadas e sms que foram faturadas, e respetivos custos, em cada periodo de faturação?
    - posso criar uma tabela para isto ou adicionar um campo nas chamadas e sms para sabermos se já foi ou nao faturado
